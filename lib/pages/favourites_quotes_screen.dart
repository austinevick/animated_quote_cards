import 'package:flutter/material.dart';

class FavouriteQuotesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Favourites')), body: Container());
  }
}
